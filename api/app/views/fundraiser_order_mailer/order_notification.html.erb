<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.6;
      color: #333333;
      margin: 0;
      padding: 0;
      background-color: #f5f5f5;
    }
    .email-wrapper {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
    }
    .header {
      background-color: #991B1B;
      padding: 30px 40px;
      text-align: center;
    }
    .header h1 {
      color: #ffffff;
      margin: 0;
      font-size: 28px;
      font-weight: 700;
      letter-spacing: 1px;
    }
    .header p {
      color: rgba(255,255,255,0.9);
      margin: 8px 0 0 0;
      font-size: 14px;
    }
    .content {
      padding: 40px;
    }
    .content h2 {
      color: #991B1B;
      margin-top: 0;
      font-size: 22px;
    }
    .order-details {
      background-color: #f8f9fa;
      border-left: 4px solid #991B1B;
      padding: 20px;
      margin: 24px 0;
      border-radius: 0 4px 4px 0;
    }
    .order-details table {
      width: 100%;
      border-collapse: collapse;
    }
    .order-details td {
      padding: 8px 0;
      vertical-align: top;
    }
    .order-details td:first-child {
      color: #666666;
      width: 140px;
      font-weight: 500;
    }
    .order-details td:last-child {
      color: #333333;
      font-weight: 600;
    }
    .order-total {
      font-size: 24px;
      color: #991B1B;
      font-weight: 700;
    }
    .items-table {
      width: 100%;
      border-collapse: collapse;
      margin: 24px 0;
    }
    .items-table th {
      text-align: left;
      padding: 12px;
      background-color: #f8f9fa;
      border-bottom: 2px solid #dee2e6;
      font-weight: 600;
      color: #333;
    }
    .items-table td {
      padding: 12px;
      border-bottom: 1px solid #e9ecef;
    }
    .items-table td:last-child {
      text-align: right;
    }
    .participant-badge {
      display: inline-block;
      background-color: #FEF3C7;
      color: #92400E;
      padding: 4px 12px;
      border-radius: 12px;
      font-size: 13px;
      font-weight: 600;
    }
    .footer {
      background-color: #f8f9fa;
      padding: 24px 40px;
      text-align: center;
      font-size: 13px;
      color: #999999;
      border-top: 1px solid #e9ecef;
    }
    .footer a {
      color: #991B1B;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="email-wrapper">
    <div class="header">
      <h1>New Fundraiser Order</h1>
      <p><%= @fundraiser.name %></p>
    </div>

    <div class="content">
      <h2>Order #<%= @order.order_number %></h2>

      <p>Hi <%= @fundraiser.contact_name || 'there' %>,</p>

      <p>Great news! A new order has been placed for your fundraiser.</p>

      <div class="order-details">
        <table>
          <tr>
            <td>Order Number</td>
            <td>#<%= @order.order_number %></td>
          </tr>
          <tr>
            <td>Customer</td>
            <td><%= @order.customer_name %></td>
          </tr>
          <tr>
            <td>Email</td>
            <td><%= @order.customer_email %></td>
          </tr>
          <% if @order.customer_phone.present? %>
          <tr>
            <td>Phone</td>
            <td><%= @order.customer_phone %></td>
          </tr>
          <% end %>
          <% if @participant %>
          <tr>
            <td>Participant</td>
            <td>
              <span class="participant-badge">
                #<%= @participant.participant_number %> - <%= @participant.name %>
              </span>
            </td>
          </tr>
          <% end %>
          <tr>
            <td>Order Date</td>
            <td><%= @order.created_at.strftime('%B %d, %Y at %I:%M %p') %></td>
          </tr>
          <tr>
            <td>Order Total</td>
            <td class="order-total">$<%= '%.2f' % (@order.total_cents / 100.0) %></td>
          </tr>
        </table>
      </div>

      <h3>Items Ordered</h3>
      <table class="items-table">
        <thead>
          <tr>
            <th>Item</th>
            <th>Qty</th>
            <th>Price</th>
          </tr>
        </thead>
        <tbody>
          <% @order.fundraiser_order_items.each do |item| %>
          <tr>
            <td>
              <%= item.product_name %>
              <% if item.variant_name.present? %>
                <br><small style="color: #666;"><%= item.variant_name %></small>
              <% end %>
            </td>
            <td><%= item.quantity %></td>
            <td>$<%= '%.2f' % (item.total_price_cents / 100.0) %></td>
          </tr>
          <% end %>
        </tbody>
      </table>

      <% if @order.has_shipping_address? %>
      <h3>Shipping Address</h3>
      <p style="background-color: #f8f9fa; padding: 16px; border-radius: 4px;">
        <%= @order.full_shipping_address.gsub("\n", "<br>").html_safe %>
      </p>
      <% end %>

      <p>You can manage this order from your fundraiser admin dashboard.</p>
    </div>

    <div class="footer">
      <p>&copy; <%= Date.today.year %> Hafaloha. All rights reserved.</p>
      <p>
        <a href="https://hafaloha.com">hafaloha.com</a>
      </p>
    </div>
  </div>
</body>
</html>
